export type Acarsdec = {
    ack: boolean
    app: {
        name: string
        ver: string
    }
    assstat: string
    block_id: string
    channel: number
    error: number
    flight: string
    freq: number
    label: string
    level: number
    mode: string
    msgno: string
    station_id: string
    tail: string
    text: string
    timestamp: number
}